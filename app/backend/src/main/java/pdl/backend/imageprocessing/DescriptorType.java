/*
  Global role of this enum:

  - Defines the types of image descriptors available (RGB, HS, GRADIENT)
*/
package pdl.backend.imageprocessing;

public enum DescriptorType {
    RGB,
    HS,
    GRADIENT
}
